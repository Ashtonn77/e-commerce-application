import { GothamShoppingValidators } from './gotham-shopping-validators';

describe('GothamShoppingValidators', () => {
  it('should create an instance', () => {
    expect(new GothamShoppingValidators()).toBeTruthy();
  });
});
